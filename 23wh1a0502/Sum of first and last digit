#include<stdio.h>
int main(){
int num,first,last,sum=0;
printf("enter any number");
scanf("%d",&num);
last=num%10;
while(num>=10){
  num=num/10;
  first=num;
sum=last+first;
  printf("sum of first and last digit of number=%d",sum);
}
